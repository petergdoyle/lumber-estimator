version: '3.8'

services:
  estimator:
    build: .
    container_name: lumber-estimator
    ports:
      - "8000:8000"
    volumes:
      - ./projects:/app/projects
      - ./archives:/app/archives
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
